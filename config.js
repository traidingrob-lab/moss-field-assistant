// Phase 2 configuration — the only file you should need to edit by hand.
// MSAL_CLIENT_ID comes from your own Azure app registration (Settings ->
// OneDrive walks you through creating one). It is NOT a secret — it's
// meant to be visible in client-side code like this, so it's safe to
// commit to a public repo.
const MSAL_CLIENT_ID = "bdddaef4-2bca-4846-a339-dbeb5e9e2b94";
